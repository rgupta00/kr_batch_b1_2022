function add(a, b){
    return a+b;
}
function mul(a, b){
    return a*b;
}

function diff(a, b){
    return a-b;
}

// module.exports=add;

// module.exports={add: add, mul: mul, dff: diff}

module.exports={ add, mul,diff}